package main;

/**
 * Classe principale del programma.
 * Questa classe funge da punto di ingresso dell'applicazione, avviando il sistema di login e registrazione.
 */
public class Main {

    /**
     * Metodo principale {@code main} che avvia l'applicazione.
     * Richiama il metodo {@link LoginSystem#main(String[])} per avviare l'interfaccia grafica del sistema di login.
     *
     * @param args Argomenti della riga di comando (non utilizzati in questo caso)
     */
    public static void main(String[] args) {
        LoginSystem.main(args); // Avvia il sistema di login
    }
}
